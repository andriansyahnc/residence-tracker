# Copy to .env for local dev
APP_PORT=3000
SESSION_SECRET=dev-residence-tracker-secret-32chars-min

# Netlify Database (auto-set on Netlify; use `netlify dev` locally)
# NETLIFY_DB_URL=postgres://...
